# 시스템 콜 (System Call)

<br>

> **1절. 커널(Kernel)**
>  - 1.1 커널이란
>  - 1.2 커널의 주요 역할
>  - 1.3 이중 모드: 사용자 모드와 커널 모드
>
> **2절. 시스템 콜(Sysyem Call)**
> - 2.1 시스템 콜이란
> - 2.2 시스템 콜의 필요성과 장점
> - 2.3 시스템 콜 처리 과정

<br>
<br>

## 1. 커널 (Kernel)

### 1.1 커널이란?

커널은 운영체제의 핵심 부분이자, 컴퓨터가 켜진 순간부터 꺼질 때까지 메모리에 항상 상주하는 프로그램이다.  

운영체제의 모든 기능이 메모리에 올라와 있을 필요는 없기 때문에, 운영체제 중 가장 필수적이고 빈번하게 사용되는 부분만을 메모리에 올려놓는데, 이 부분이 바로 커널이다.  

커널은 응용 프로그램과 하드웨어 사이의 인터페이스 역할을 하며, 응용 프로그램이 하드웨어 자원을 직접 사용할 수 없도록 하고, 대신 커널을 통해 안전하고 효율적으로 자원을 사용할 수 있도록 중재한다.

<br>

### 1.2 커널의 주요 역할

커널은 컴퓨터의 핵심 자원들을 관리하고 제어하는 중추적인 역할을 수행한다.

- **프로세스 관리:**

- **메모리 관리**

- **파일 시스템 관리**

- **장치 관리**

- **네트워크 관리**

- **인터럽트 처리**

<br>

### 1.3 이중 모드: 사용자 모드와 커널 모드

운영체제는 시스템읩 보안과 안정성을 위해 CPU를 두 가지 모드로 구분해서 사용한다.

- **사용자 모드(User Mode)**
  - 일반적인 응용 프로그램이 실행되는 모드이다.
  - 이 모드에서는 하드웨어 자원(디스크, I/O 장치 등)에 직접 접근할 수 없다.
  
**커널 모드(Kernel Mode)**
  - 운영체제, 즉 커널이 CPU를 사용하는 모드이다.
  - 이 모드에서는 모든 하드웨어 자원에 대한 접근 권한이 있으며, 특권명령을 실행할 수 있다.
  - 시스템의 중요한 작업은 반드시 커널 모드에서 이루어진다.

<br>

CPU는 **모드 비트**(Mode Bit)라는 플래그를 통해 현재 어떤 모드에서 실행 중인지를 구분한다. (예: `1`이면 사용자 모드, `0`이면 커널 모드)

<br>

> 💡 **특권 명령(privileged instruction)**
>
> 오직 커널 모드에서만 실행할 수 있는 명령어를 말한다.  
> 사용자 모드에서는 실행할 수 없으며, 만약 시도하면 예외(인터럽트)가 발생하여 시스템이 이를 막는다.
>
> 예: `I/O 명령`, `인터럽트 조작`, `메모리 관리`, `CPU 제어`, `시스템 종료`

<br>
<br>


## 시스템 콜 (System Call)

### 2.1 시스템 콜이란

시스템 콜은 **응용 프로그램이 커널의 기능을 사용하기 위해 커널에게 서비스를 요청하는 인터페이스**이다.    

사용자 모드에서 실행되는 응용 프로그램은 하드웨어에 직접 접근할 수 없기 때문에 파일 입출력, 프로세스 생성, 네트워크 통신 등과 같은 **시스템 자원을 사용하는 작업**은 반드시 **커널에게 요청**해야한다.  

이때 이러한 요청을 **커널에게 전달하는 유일한 통로가 바로 시스템 콜**이다.

![image](https://github.com/user-attachments/assets/e3c9f8bc-2563-49e8-8d5f-abd2eb6418fb)

> **[응용 프로그램]** → (시스템 콜 호출) → **[커널]** → (특권 명령 실행) → **[하드웨어]**

<br>

### 2.2 시스템 콜의 필요성 및 장점

- **보안 및 안정성 강화**
  - 사용자 프로그램이 하드웨어에 직접 접근하지 못하게 막고, 커널이 대신 자원 접근을 통제함으로써 시스텀 전체를 보호한다.

- **추상화된 인터페이스 제공**
  - 시스템 콜은 복잡한 하드웨어 동작을 감추고, 간단한 함수 형태로 제공된다.
  - `read()`, `write()`는 실제로는 복잡한 디바이스 제어와 메모리 관리 포함 

- **이식성 향상**
  - 응용 프로그램은 시스템 콜만 사용하면 되므로, 운영체제가 달라서 같은 API가 구현되어 있다면 재사용성이 높다.

<br>

### 2.3 시스템 콜의 처리 과정










